spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///testdb
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

minio:
  url: http://localhost:9000
  accessKey: test-access-key
  secretKey: test-secret-key
  buckets:
    fileBucket:
      name: test-files

app:
  api:
    base-url: http://localhost:8080
  default:
    default-admin:
      username: DefaultAdmin
      email: dafault@default.com
      password: DeF@uLtADm1N_P@sswOrd
      quota-size: 100GB

management:
  endpoints:
    web:
      exposure:
        include: "*"